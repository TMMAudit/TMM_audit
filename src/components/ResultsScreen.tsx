// ResultsScreen is no longer used directly - kept for backward compatibility
// The new flow uses SectionResultsScreen and CompositeResultsScreen
const ResultsScreen = () => null;
export default ResultsScreen;
